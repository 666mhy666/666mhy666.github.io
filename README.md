# Heyang Ma — Portfolio

Static GitHub Pages site for eight independent UCLA projects. Publish as `666mhy666.github.io`, then enable Pages from the main branch root. No build service is required. Source data and model weights are excluded.

The sibling project repositories are the source of project files. Regenerate the site with the private local `portfolio/tools/build_release.py` after changes.
